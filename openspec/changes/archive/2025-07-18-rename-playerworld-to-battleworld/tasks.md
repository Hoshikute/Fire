## 1. 重命名

- [x] 1.1 将 `PlayerWorld.cs` 文件重命名为 `BattleWorld.cs`，类名改为 `BattleWorld`
- [x] 1.2 更新 `TPBattleContext.cs` 中所有 `PlayerWorld` 引用为 `BattleWorld`
- [x] 1.3 更新 `ClimbConfig.cs` 注释中的 `PlayerWorld` 为 `BattleWorld`

## 2. 验证

- [x] 2.1 确认编译通过
