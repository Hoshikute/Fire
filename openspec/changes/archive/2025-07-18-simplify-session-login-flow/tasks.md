## 1. 协议与会话约束整理

- [x] 1.1 盘点当前 `PlayerLoginMsg_s` / `PlayerLoginMsg_c` 的字段用途，确定匿名 session 登录所需最小字段集
- [x] 1.2 调整登录协议定义，使客户端不再上传权威 `playerID`，并让响应包含服务端分配的临时玩家标识
- [x] 1.3 检查登录后依赖玩家标识的客户端运行时状态与日志输出点，确认都可切换到服务端返回值

## 2. 服务端匿名玩家创建

- [x] 2.1 修改 `LoginService`，在登录请求到达时基于当前 session 创建临时 `Player`
- [x] 2.2 为临时 `Player` 生成运行期唯一标识，并绑定到 `session.player`
- [x] 2.3 去除登录流程对客户端传入 `playerID` 和数据库加载的前置依赖
- [x] 2.4 校验 `MatchService` 与对局初始化仍通过 `session.player` 正常工作

## 3. 客户端 LoginWindow 接入

- [x] 3.1 调整 `LoginWindow` 文案和交互语义，使其表达“进入游戏/匿名入场”而不是账号密码登录
- [x] 3.2 修改客户端登录调用，点击按钮后发起匿名登录请求，不再上传自定义身份
- [x] 3.3 在客户端保存并使用服务端返回的临时玩家标识，替代本地输入名称的身份角色

## 4. 限制说明与验证

- [x] 4.1 明确纯 session 方案不保证断线重连恢复，并检查相关逻辑不会制造错误预期
- [ ] 4.2 验证单客户端从 LoginWindow 进入游戏的完整链路
- [ ] 4.3 验证双客户端匹配、进房和基础帧同步链路在匿名登录下可正常运行
