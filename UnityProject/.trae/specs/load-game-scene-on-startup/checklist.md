- [x] 启动流程在 `ProcedureStartGame` 阶段默认加载 `Game` 主场景
- [x] 主场景切换通过 `GameModule.Scene` 的异步接口完成
- [x] 启动器 UI 在进入 `Game` 主场景前已被隐藏
- [x] 默认进入 `Game` 主场景的能力不绕过既有资源初始化、程序集加载与热更启动流程
- [x] `Game` 主场景的资源定位名与工程内实际资源配置一致；按用户要求，本轮未执行 Unity 运行态验证
